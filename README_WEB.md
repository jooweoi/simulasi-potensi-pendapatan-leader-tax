# Simulasi Potensi Pendapatan Leader Tax - Web

Versi ini disiapkan untuk deploy sebagai website Flask di Render.

## Render

- Build Command: `pip install -r requirements.txt`
- Start Command: `gunicorn app:app`

Gunakan repository baru untuk versi Leader Tax supaya versi lama tetap aman.
